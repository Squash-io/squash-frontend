import styled from '@emotion/styled';
import { COLORS } from '../../../constants/Color/Color';

export const LoginButtonContainer = styled.button`
  display: flex;
  padding: 0.9rem 7rem;
  align-items: center;
  background-color: ${COLORS.baseColors.gray950};
  gap: 3.2rem;
  border-radius: 1.2rem;
  cursor: pointer;
`;
